apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gke-node-startup.fullname" . }}-test-connection"
  labels:
    {{- include "gke-node-startup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gke-node-startup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
